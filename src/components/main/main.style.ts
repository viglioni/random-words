import { makeStyles } from '@material-ui/core'

export const useStyles = makeStyles(() => ({
  selectGrid: { padding: '5%' },
  container: { width: '100%' },
  main: { maxWidth: '500px' },
  words: { padding: '0 5%' },
  button: { marginTop: '5%' },
}))
